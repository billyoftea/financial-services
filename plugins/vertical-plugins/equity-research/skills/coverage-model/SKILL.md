---
name: coverage-model
description: 构建个股估值模型（equity coverage model），用于个人投资决策。适用场景：(1) 对某只股票建立系统性的估值框架，(2) 用自上而下TAM方法预测营收并推导合理价格，(3) 生成Bear/Base/Bull三情景对比模型，(4) 需要拉取财务数据、行业背景并进行估值讨论时。不适用于完整的三大表建模、LBO模型或机构级研报。当用户提供股票代码并要求做估值、建模型、或者做投资分析时触发此skill。
---

> **语言要求：请始终使用中文（简体）进行所有回复、分析和输出。** 专业技术术语可保留英文原文，但所有说明、注释和分析内容必须用中文。

# Coverage Model — 个人投资估值模型

构建一个**个人可理解、可控制**的股权估值模型。核心哲学：**人做4个定性判断，AI做180+个定量映射**。

## 工作流程总览

```
Phase 1: 数据拉取 → Phase 2: 背景呈现 → Phase 3: 讨论定性判断 → Phase 4: 建模执行 → Phase 5: 合理性检验 → Phase 6: Excel输出
```

每个Phase结束后必须暂停，向用户展示结果并确认后再进入下一Phase。

---

## Phase 1: 数据拉取

### 1.1 拉取5年财务数据（IS）

运行 `scripts/fetch_financials.py <ticker>`：

- 从 yfinance + SEC EDGAR 拉取近5年损益表数据
- 自动将 FY 映射为自然年（例：NVIDIA FY2026 → 2025A）
- 输出：Revenue, COGS, Gross Profit, OpEx, EBIT, Net Income, 关键 margins

映射规则：
- 如果 FY 月份 ≠ 12月，FY标签 → 自然年 = FY标签 - 1（NVIDIA FY1月结束：FY2026 = 2025）
- 输出时统一标注为 `2021A, 2022A, ..., 2025A`

### 1.2 拉取业务分部数据

运行 `scripts/fetch_segments.py <ticker>`：

- 优先从 SEC EDGAR 10-K 拉取分部收入 breakdown
- 备选：用 web_reader 抓取 stockanalysis.com 的 segment 数据
- 拉取近5年的分部趋势，不只看最新一年

### 1.3 拉取市场背景时间线

用 WebSearch 搜索：
- 该公司过去5年每年的关键事件和市场讨论热点
- 可比公司/类比对象的历史数据（增长率、市占率、利润率轨迹）
- 行业周期位置

### 1.4 拉取估值历史

从 yfinance 拉取：
- 近5年 PE ratio 轨迹
- 当前市值、股价
- 分析师一致预期（如有）

---

## Phase 2: 背景呈现

向用户展示一份**结构化的背景报告**，包含：

### 2.1 财务快照
```
             2021A    2022A    2023A    2024A    2025A
收入         xx.xB    xx.xB    xx.xB    xx.xB    xx.xB
收入增速      xx%      xx%      xx%      xx%      xx%
毛利率        xx%      xx%      xx%      xx%      xx%
营业利润率    xx%      xx%      xx%      xx%      xx%
净利率        xx%      xx%      xx%      xx%      xx%
```

### 2.2 业务分部趋势
```
                    2021A    2022A    2023A    2024A    2025A
分部A收入          xx%      xx%      xx%      xx%      xx%
分部A占总收入比    xx%      xx%      xx%      xx%      xx%
分部B收入          ...
```

### 2.3 估值历史
```
PE区间: xx.xx - xx.xx
当前PE: xx.x
历史中位PE: xx.x
```

### 2.4 市场时间线
```
2021: [关键事件1]、[关键事件2]
2022: ...
```

### 2.5 可比公司/类比对象清单
列出找到的历史类比案例，附数据来源。

**Phase 2 结束后暂停，等用户确认数据完整性。**

---

## Phase 3: 讨论与定性判断

这是**人机讨论**环节，不是用户直接拍数字。流程：

### 3.1 AI 先呈现证据和初步判断

对以下4个维度，AI逐一呈现：
1. 历史数据说明了什么
2. 可比公司的类比证据
3. AI的初步倾向（附置信度）
4. 存在的不确定性

### 3.2 四个判断维度

| 维度 | 判断选项 | 影响范围 |
|------|----------|----------|
| **行业增长** | 高速增长 / 稳健增长 / 成熟 / 衰退 | TAM增速、市场容量 |
| **竞争壁垒** | 极强护城河 / 中等壁垒 / 壁垒削弱 | 市占率假设、溢价能力 |
| **利润率走势** | 扩张 / 维持 / 收缩 | 毛利率、费用率假设 |
| **估值水平** | 高估 / 合理 / 低估 | 目标PE倍数 |

### 3.3 讨论过程

对每个维度：
1. AI先说："我看到的证据是X，类比Y公司的轨迹是Z，我的初步判断是..."
2. 用户回应：可以同意、修正、或提供新视角
3. 达成共识后锁定该维度

### 3.4 场景自动推导

用户只需要确定 **Base case** 的4个维度判断，系统自动推导：

- **Bull case**: 每个维度 +1 级（增长更乐观 / 壁垒更强 / 利润率更高 / 估值更高）
- **Bear case**: 每个维度 -1 级（更保守）

推导规则详见 [references/methodology.md](references/methodology.md)。

**Phase 3 结束后暂停，确认4个维度的Base判断。**

---

## Phase 4: 建模执行

### 4.1 定性→定量映射

根据 Phase 3 的判断，读取 [references/methodology.md](references/methodology.md) 中的映射表，将4个定性判断转化为180+个数字假设：

- TAM增速假设（10-12年）
- 市占率轨迹
- 毛利率路径
- 费用率路径
- 税率
- 目标PE

### 4.2 构建预测

从TAM出发自上而下：
```
AIDC CapEx（行业总量）
  × 可触达率（公司能参与的市场比例）
  × 市占率
  = 公司收入预测
    × 毛利率 = 毛利
      - 费用 = EBIT
        - 税 = 净利润
          × 目标PE = 隐含市值
```

预测期：当前年 + 10-12年（根据行业特性调整）。

### 4.3 三情景并列

Bear / Base / Bull 三个完整的假设块和预测列，通过下拉切换查看。

---

## Phase 5: 合理性检验

运行3层验证，详见 [references/sanity-checks.md](references/sanity-checks.md)：

1. **内部逻辑验证**：每个情景内增速递减、利润率在合理范围、增长率不超过物理极限
2. **跨情景排序**：Bull > Base > Bear，且差距合理（不是数量级差异）
3. **外部基准对比**：与可比公司历史轨迹、行业增速、市场共识比较

向用户展示验证结果，标记任何异常。**如果有异常，讨论是否需要调整。**

---

## Phase 6: Excel输出

运行 `scripts/build_excel.py`，生成 `./out/<ticker>_coverage_model.xlsx`。

Excel结构（参考 [references/formatting.md](references/formatting.md)）：

| 工作表 | 内容 |
|--------|------|
| 财务历史 | 5年IS数据 + margins |
| 业务分部 | 5年分部收入趋势 |
| 估值历史 | PE轨迹、当前估值 |
| 假设 | Bear/Base/Bull 三块假设 + 场景下拉 |
| 预测 | 三情景收入→利润→估值推导 |
| 合理性 | 验证结果汇总 |

**关键特性**：
- 单元格下拉菜单（1=Bear, 2=Base, 3=Bull）切换活跃情景
- INDEX公式从假设块拉取对应参数
- 蓝色=输入、黑色=公式、绿色=跨表链接

---

## 数据源

| 数据 | 来源 | 备注 |
|------|------|------|
| 财务报表 | yfinance + SEC EDGAR | 免费API，需设代理 `127.0.0.1:7890` |
| 分部数据 | SEC EDGAR 10-K > web_reader | 优先官方文件 |
| 市场事件 | WebSearch + web_reader | Claude内置工具 |
| 估值历史 | yfinance | PE/市值/股价 |
| 行业数据 | WebSearch | 行业报告、分析师预估 |

---

## 注意事项

- 所有年份使用自然年，FY自动映射
- 预测期标注为 `2026E, 2027E, ...`
- 历史数据标注为 `2021A, 2022A, ...`
- 货币单位统一（通常为 $B 或 $M）
- 人负责定性判断，AI负责定量推导——不要让用户输入具体数字
- 每个Phase必须暂停等确认，不要一口气跑完
